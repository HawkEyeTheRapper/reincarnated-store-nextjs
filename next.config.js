/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['hawkeyetherapper.shop', 'assets.omniversal.vip'],
  },
};

module.exports = nextConfig;
